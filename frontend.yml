apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: app # lesson3-3
spec:
  selector:
    matchLabels:
      app: front
  replicas: 1
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - name: multitool
        image: wbitt/network-multitool
        ports:
        - containerPort: 8080
          name: port-multitool
        env:
          - name: HTTP_PORT
            value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: app # lesson3-3
spec:
  selector:
    app: front
  ports:
    - name: port-frontend-svc
      protocol: TCP
      port: 9002
      targetPort: port-multitool
